# Незашифрованный исходник — только локально, в Git/GitHub не попадает
source.html

# Пароль Staticrypt — только локально
.staticrypt-password

# Временные файлы сборки
.build-tmp/

# Мастер-копия dev-контролов — только локально
dev/
